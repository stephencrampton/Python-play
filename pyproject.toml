[project]
name = "python-play"
version = "0.1.0"
description = "Multi-process data pipeline demo using TCP sockets"
authors = [{name = "Stephen Crampton"}]
requires-python = ">=3.8"
dependencies = []

[tool.uv]
python-preference = "managed"
